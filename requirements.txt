# MinerU 3.4 + 文献资产库依赖
# 安装: conda activate mineru && pip install -r requirements.txt

# MinerU (核心)
mineru[all]>=3.4.0

# API
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# HTTP
requests>=2.28.0

# 工具
loguru>=0.7.0
pydantic>=2.0.0
PyMuPDF>=1.24.0
filelock>=3.12.0
orjson>=3.10.0

# 测试
pytest>=8.0.0
pytest-xdist>=3.8       # 并行验收(agent_acceptance --jobs);带 execnet
jsonschema>=4.0.0

# 运行支撑(agent_acceptance 进程树清理已直接使用,显式声明)
psutil>=7.0

# 注：重构后已移除 chromadb / sentence-transformers（不再做向量检索）
