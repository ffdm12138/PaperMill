"""Workspace facts layer: evidence, readiness, lifecycle, discovery receipt.

Sits between library (ledger) and the ingest/discovery orchestrators so
neither side needs the other to inspect a single workspace.
"""
