"""Discovery v4 reporting — ReportBuilder and report models.

Reports are generated from strict V4 contracts and durable progress
facts only — never from HTTP status codes or items returned.

Import directly from submodules:
    from src.discovery.reporting.report_builder import ReportBuilder
"""
