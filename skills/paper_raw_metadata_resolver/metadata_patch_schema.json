{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "paper_raw metadata patch (resolver)",
  "description": "Patch produced by the paper_raw_metadata_resolver skill. It is merged into the existing metadata via src/services/v2_library.py::merge_missing_metadata(), which fills ONLY empty fields and preserves trusted non-empty fields. Conflicts are recorded as warnings, never overwritten. DOI must come from the PDF/Markdown/filename or an authoritative network source — never fabricated. STRICT: additionalProperties is false. Legacy fields are FORBIDDEN here: title.short_zh, title.translated_zh, abstract, keywords, notes, bibtex, citation_key, source.raw_record (use source.raw_record_path only), content_title, md_title_candidates. Chinese titles belong in catalog (content_identity.content_title_zh), never in metadata.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "warnings": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Suspected errors in existing non-empty metadata fields. Recorded as notes, never auto-corrected."
    },
    "title": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "original": { "type": "string" },
        "subtitle": { "type": "string" }
      }
    },
    "authors": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "properties": {
          "full_name": { "type": "string" },
          "family": { "type": "string" },
          "given": { "type": "string" },
          "orcid": { "type": "string" },
          "affiliation": { "type": "string" }
        }
      }
    },
    "first_author": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "family": { "type": "string" },
        "display": { "type": "string" }
      }
    },
    "year": { "type": ["integer", "string", "null"] },
    "container": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "journal": { "type": "string" },
        "booktitle": { "type": "string" },
        "conference": { "type": "string" },
        "series": { "type": "string" },
        "publisher": { "type": "string" },
        "institution": { "type": "string" },
        "school": { "type": "string" }
      }
    },
    "publication": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "volume": { "type": "string" },
        "number": { "type": "string" },
        "issue": { "type": "string" },
        "pages": { "type": "string" },
        "article_number": { "type": "string" },
        "edition": { "type": "string" }
      }
    },
    "identifiers": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "doi": { "type": "string" },
        "issn": { "type": "string" },
        "openalex_id": { "type": "string" },
        "crossref_id": { "type": "string" }
      }
    },
    "links": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "url": { "type": "string" },
        "publisher_url": { "type": "string" },
        "repository_url": { "type": "string" }
      }
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "kind": { "type": "string" },
        "provider": { "type": "string" },
        "query": { "type": "string" },
        "retrieved_at": { "type": "string" },
        "raw_record_path": { "type": "string" }
      }
    }
  }
}
