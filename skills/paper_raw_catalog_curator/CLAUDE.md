# Catalog Curator v3.2 boundary

Read `SKILL.md` and `catalog_schema.json` completely. Accept only numeric workspaces with a valid task envelope. Write only `<paper_number>.catalog.json`.

Catalog is a complete paper-content archive and generates the human-readable `paper_id`; Metadata remains the frozen citation record. Never mirror structured bibliography, mutate inputs, rename assets, update workflow state, or write formal/index/transaction data.
